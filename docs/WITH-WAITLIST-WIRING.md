The live WITH. join form posts to:

`https://ydpwxemszizsmgxewjvr.supabase.co/functions/v1/with-waitlist`

The browser must never receive a Supabase secret or service role key. The public Edge Function validates input and writes through backend credentials only.